// const React = require("react");
//
// module.exports = React.forwardRef((props: unknown) => React.createElement("svg", props));

const React = require("react");

module.exports = {
    ReactComponent: "svg",
};
